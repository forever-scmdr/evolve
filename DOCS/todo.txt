Протестировать - страницы

+1)  Простой вывод айтемов на страницы (все виды - tree list single ancestor)
+2)  Работа фильтра с обычными параметрами
+3)  Работа фильтра с параметрами потомков айтемов
+4)  Работа фильтра с параметрами предков айтемов
+5)  Опции фильтра
+6)  Простая группировка по значению параметра
+7)  Группировка по дополнительным параметрам
+8)  Группировка совместно с фильтром
9)  Вывод айтемов с учетом ассоциации (не первичной)
10) Поиск айтемов с переданными через URL ID и значением параметра
11) Применение критерия пользователя и группы пользователей
12) Поиск страницы по умолчанию для определенного айтема (айтем сначала загружается по уникальному ключу, потом
	ищется страница по умолчанию для айтема этого типа)
13) Запрет вывода защищенных файлов
14) Инпуты
15) Длительные операции (прогрессы)

Протестировать - модель

1)  Журнал обновлений айтема (computed параметры)
2)  Оптимизация сохранения айтема (когда айтем обновляется, но изменения параметров не было)


Интерфейс админа

Для каждого айтема добавить (можно в списке сабайтемов, можно на странице айтема, можно что-то там, что-то там, не знаю
как лучше):
1)  Скрыть/показать айтем. (чтобы скрытые айтемы как-то выделялись в списке айтемов слева)
2)  Изменить группу владельцев айтема (выбрать группу из списка и нажать ОК)
3)  Присвоить айтем пользователю (выбрать пользователя из списка возможно с поиском, нажать ОК)
4)  Скрыть/раскрыть файлы айтема (для общего анонимного доступа)

Создание связей:
1)  Группировать доступные для создания сабайтемы по группам (каждая группа - своя ассоциация). Это касается верхней
	части списка айтемов, где создание (зеленые плюсы)
2)  Нажатие на плюс не в ассоциации "Первичная иерархия" вызывает окошко с выбором прикрепляемого айтема, аналогично
	существующим связям уровня или ассоциациям, только добавить еще возможность поиска.
3)  Группировать уже существующие сабайтемы по ассоциациям аналогично пункту 1.
4)  В списке сабайтемов добавить страницы и возможность выводить сабайтемы в обратном порядке
5)  Связи верхнего уровня заменить на что-то похожее на пункт 2 только наоборот, выбирается ассоциация и ищется новый
	родительский айтем. Возможно сначала ищется новый родительский, а потом выбирается ассоциация. Или рядом с каждым
	потенциальным родительским в списке выводить все возможные ассоциации для него.

Корзина:
1)  Где-то должна быть ссылка на корзину
2)  В корзине должен быть скисок айтемов и ссылка восстановить
3)  Должна быть ссылка очистить.

Корневые страницы админов разных групп:
1)  В pages.xml сделать админские страницы




+ДОДЕЛАТЬ (доделано, нет необходимости писать как в примере, параметры группируются автоматически)

+1) Привести критерий в фильтре по однинаковым параметрам к виду
	<parameter name="param">
	    <var value="..." sign="&gt;"/>
	    <var value="..." sign="!="/>
	</parameter>
	Это нужно для того, чтобы избавиться от двойного соеднинения с одной и той же таблицей в случае
	<parameter name="param"><var value="..." sign="&gt;"/></parameter>
	<parameter name="param"><var value="..." sign="!="/></parameter>
	Аналогично можно избавиться от двойного соединения с таблицей сортировки, в случае если в фильтре уже есть
	критерий по тому же параметру, что и сортировка. Такой бывает часто в случае с ценой (не более определенной с
	сортировкой по возрастанию)